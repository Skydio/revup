matrix:
  platform: ["debian11", "macos", "ubuntu2004"]
  bazel: ["8.x"]

tasks:
  verify_targets:
    name: Build the revup wheel
    platform: ${{ platform }}
    bazel: ${{ bazel }}
    build_targets:
      - "@revup//:revup"
